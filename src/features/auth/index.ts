// Auth feature module
// Phase 1: email/password login, register ✓
// Phase 5: Google OAuth — find-or-create user, account linking ✓
//
// Auth is configured in src/auth.ts (NextAuth v5).
// The OAuthAccount model (prisma/schema.prisma) stores linked OAuth accounts
// so a single user can sign in with both email/password and Google.

export {};
