/** Default visits per API / SSR page slice */
export const DEFAULT_VISIT_PAGE_SIZE = 40;

export const MAX_VISIT_PAGE_SIZE = 100;

/** Max city pins rendered on the world map (most recently visited cities). */
export const MAX_MAP_CITY_MARKERS = 150;

/** Max visits accepted in one JSON import request */
export const MAX_IMPORT_VISITS = 500;

/** Max raw JSON body size for visit import (bytes) */
export const MAX_IMPORT_BODY_BYTES = 2_000_000;
